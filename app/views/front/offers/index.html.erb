<div class='row'>
  <h3> Услуги </h3>
  <% if @offers.empty? %>
    <h5><%= t(:empty_page) %></h5>
  <% else %>
    <ul class="accordion" data-accordion data-allow-all-closed="true">
      <%= render @offers %>
    </ul>
  <% end %>
</div>
